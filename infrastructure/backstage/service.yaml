apiVersion: v1
kind: Service
metadata:
  name: backstage
  namespace: backstage
  labels:
    app: backstage
    app.kubernetes.io/name: backstage
    app.kubernetes.io/part-of: idp
spec:
  selector:
    app: backstage
  ports:
  - name: http
    port: 80
    targetPort: 7007
    protocol: TCP
  type: ClusterIP