# This file will be generated by Flux CLI during installation
# Use the following command to generate it:
# flux install --export > flux-system/gotk-components.yaml

# For now, this is a placeholder that will be replaced during installation
apiVersion: v1
kind: Namespace
metadata:
  name: flux-system
  labels:
    app.kubernetes.io/part-of: flux
    app.kubernetes.io/component: flux-system